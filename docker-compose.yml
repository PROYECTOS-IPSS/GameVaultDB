services:
  app:
    build: .
    container_name: gamevault
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./prisma/dev.db
    volumes:
      - db-data:/app/prisma

volumes:
  db-data:
